
package:
    name:       m3d
    version:    "0.0.0"

source:
    path: .

requirements:
    build:
        - {{ compiler('c') }}
        - {{ compiler('cxx') }}
        - {{ compiler('fortran') }}
        - mpi
        - openmpi
        - python
    host:
        - {{ compiler('c') }}
        - {{ compiler('cxx') }}
        - {{ compiler('fortran') }}
        - mpi
        - openmpi
        - python
    run:
        - {{ compiler('c') }}
        - {{ compiler('cxx') }}
        - {{ compiler('fortran') }}
        - mpi
        - openmpi
        - python


